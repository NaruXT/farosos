{
  "name": "farosos-backend",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "firebase emulators:exec --project farosos-rules-test --only firestore,auth \"node --test tests/*.test.mjs\"",
    "verify-dedup": "node scripts/verify-dedup.mjs",
    "generate-beacon-auth-vectors": "node scripts/generate-beacon-auth-vectors.mjs"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^4.0.1",
    "@noble/curves": "^2.3.0",
    "firebase": "^11.0.0",
    "firebase-admin": "^13.0.0",
    "firebase-tools": "^13.29.0"
  }
}

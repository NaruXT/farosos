<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Pre-Android 12 (API < 31): BLUETOOTH/BLUETOOTH_ADMIN + ubicación son
         los permisos que el sistema exige para advertising/scanning/GATT BLE. -->
    <uses-permission android:name="android.permission.BLUETOOTH" android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" android:maxSdkVersion="30" />
    <!-- Android 12+ (API 31+): permisos runtime dedicados. `neverForLocation`
         evita tener que pedir también ubicación — no derivamos posición de
         los resultados del scan, la latitud/longitud viaja en el payload. -->
    <uses-permission
        android:name="android.permission.BLUETOOTH_SCAN"
        android:usesPermissionFlags="neverForLocation" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADVERTISE" />
    <!-- Requerido para `BluetoothDevice.connectGatt`/`readCharacteristic`
         (ticket #11, lectura de beacons de iOS vía GATT) — sin este permiso
         cualquier conexión GATT lanza SecurityException en API 31+. -->
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />

    <!-- Requerido por `ConnectivityManager.registerNetworkCallback`
         (ticket #17/#21, detección de conectividad real de la Máquina B). -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!-- Requerido por Firebase Auth/Firestore (ticket #30, subida de
         `participants/{device_id_hash}`) — primer código de esta app que
         hace requests HTTP reales. -->
    <uses-permission android:name="android.permission.INTERNET" />

    <uses-feature android:name="android.hardware.bluetooth_le" android:required="true" />

    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:theme="@style/Theme.Farosos">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>

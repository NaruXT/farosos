# El heap por defecto del daemon de Gradle (512 MiB) no alcanza para que D8
# dexee el grafo de dependencias de Firebase + Google Play Services agregado
# en el ticket #30 (`mergeExtDexDebug` falla con OutOfMemoryError sin esto).
org.gradle.jvmargs=-Xmx2560m -XX:MaxMetaspaceSize=1024m
